{% if page.content != blank %}
  <section class="page-body-section">
    <div class="page-body-inner rte">{{ page.content }}</div>
  </section>
{% endif %}

{% schema %}
{
  "name": "Seiteninhalt (aus Admin)",
  "settings": [
    { "type": "paragraph", "content": "Rendert den Body der Page aus dem Shopify-Admin." }
  ],
  "presets": [{ "name": "Seiteninhalt" }]
}
{% endschema %}
