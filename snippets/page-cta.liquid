<section class="cta reveal">
  <div>
    <p class="cta-label">{{ label | default: 'Bereit loszulegen?' }}</p>
    <h2 class="cta-title">{{ title | default: 'Lass uns dein Projekt besprechen.' }}</h2>
  </div>
  <a href="{{ url | default: '/pages/kontakt' }}" class="btn-orange">{{ btn | default: 'Kontakt aufnehmen' }} &nbsp;→</a>
</section>
